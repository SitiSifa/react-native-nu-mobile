import React from 'react'
import {
  StyleSheet,
} from 'react-native'

const styles = StyleSheet.create({
	imageLogo:{
		width: 200,
		height: 200,
		justifyContent:'center',
		alignItems:'center',
	},
	imageContent:{
		width: 80,
		height: 80,
		justifyContent:'center',
	}
});

export default styles
